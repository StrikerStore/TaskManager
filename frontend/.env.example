# Where the web server forwards /api/* (see next.config.ts). Locally the dev API;
# on Railway the API service's private address, e.g. http://backend.railway.internal:4000
API_INTERNAL_URL=http://localhost:4000
